# linc-overmann-target

Throwaway test repo for Overmann v1 stage testing. See `~/projects/LINC/docs/superpowers/specs/2026-05-11-overmann-v1-design.md` §Rollout plan > Stages 1 and 2 for context.

Hand-crafted Overmann-labeled issues will land here during Stages 1 and 2. PRs opened by `/linc-circuit --auto` runs will modify `src/canary.py` and `tests/`. This repo has no production users and may be force-reset at any point.
